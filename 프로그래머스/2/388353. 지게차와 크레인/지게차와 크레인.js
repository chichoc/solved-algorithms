function solution(storage, requests) {
    let answer = 0;
    const board = storage.map(row => [...row])
    console.log(board)
    for (const request of requests) {
        if (request.length === 1) {
            
        } else {
            // 크레인
        }
    }
    
    return answer;
}